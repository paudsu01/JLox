// The following program takes number(input) from the user and adds current time to it

print "Hi, what's your name?";

// "input" native function used
var x = input();

print "Input a number: ";
var y = input();

// "number" native function used; raises error if x cannot be converted to number
var numY = number(y);

// "clock" native function used
print "Hey " + x + ", the current time: ";
print clock();
print "Hey " + x + ", your input + 5: ";
print numY + 5;
